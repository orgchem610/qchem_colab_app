# =============================================================================
# requirements.txt  (qchem_colab_app — HF/3-21G 最小プロトタイプ)
# -----------------------------------------------------------------------------
# 【バージョン指定の考え方】
#   Pythonの科学技術計算系ライブラリは、メジャーバージョンが上がる際に
#   後方互換性のない変更(API変更)が入ることが珍しくありません
#   (例: ipywidgets 7→8 でファイルアップロードウィジェットの返り値の型が
#    dict から tuple に変わっており、実際に gui.py 側で両対応の吸収コードを
#    書いています)。
#
#   そのため、このファイルでは「今動くコードが将来の未知のメジャー
#   バージョンで壊れるリスク」と「バグ修正版が出ても指定が古いままで
#   困るリスク」のバランスを取り、パッケージごとに以下の使い分けをしています。
#
#     - このプロジェクトのコードが具体的なAPIの形(戻り値の型・関数シグネチャ等)
#       に強く依存しているもの(pyscf, geometric, ipywidgets)
#         → 下限だけでなく上限も指定し(例: ">=2.8,<3")、
#           次のメジャーバージョンを自動では取り込まないようにする。
#     - 使っている機能がごく基本的で、メジャーバージョンが上がっても
#       壊れる可能性が低いもの(PyYAML, py3Dmol, plotly)
#         → 下限のみ指定する。
#
#   ただし、これはあくまで**初回インストール時**の目安です。
#   一度Colab上で正常に動作した後は、`requirements-lock.txt`
#   (pip freezeで生成される完全固定リスト)を優先して使う設計になっており
#   (ColabApp.ipynbのSetup Section参照)、そちらが実際の再現性を担保します。
#   「将来動かなくなるリスク」への最終的な備えは、このrequirements.txtの
#   指定の緩さ・厳しさではなく、requirements-lock.txtをGitにコミットして
#   都度残しておくことです。
# =============================================================================

# --- 計算エンジン本体 --------------------------------------------------------
pyscf>=2.8,<3

# 構造最適化(内部座標法)。PySCFのgeomopt機能が依存する外部パッケージ
geometric>=1.0,<2

# --- 設定ファイル(yaml.safe_load等、ごく基本的な機能のみ使用) ---------------
PyYAML>=6.0

# --- GUI(FileUpload.valueの型が7→8で変わっているため上限も指定) -----------
ipywidgets>=8.1,<9

# --- 可視化(基本的なAPIのみ使用) ----------------------------------------------
py3Dmol>=2.1
plotly>=5.20
# エネルギー収束グラフを静的PNG画像として書き出すために使用(kaleido)。
# Colab上のipywidgets.Output()内ではplotlyのインタラクティブ表示(JS実行)が
# 描画されないことがあったため、静的画像に変換する方式に変更した。
# kaleido 1.0以降はAPIが刷新されplotly 6.1.1以降が必須になり、
# 現在使用しているplotly 5.x系とは非互換(Chrome同梱もされなくなった)ため、
# 旧APIのまま安定していた最終版 0.2.1 を明示的に固定する。
kaleido==0.2.1

# --- GPU4PySCF について -------------------------------------------------------
# ColabのCUDAドライババージョンに強く依存するため、このファイルには含めず、
# ColabApp.ipynb のSetup Section内で個別にインストールを試みます
# (失敗しても qcapp/engine.py がCPU実行に自動フォールバックします)。
