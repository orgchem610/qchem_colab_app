# CHANGELOG

このファイルには、バージョン(Gitタグ)ごとの変更点を日本語で簡潔に記録します。
「前のバージョンに戻したときに何が失われるか」が分かることを目的としています。

> **今後の方針について**: 次回のリリース(develop→mainへのマージ)からは、
> このファイルではなく、リリースコミット・タグのメッセージとGitHubのReleases機能に
> 記録を一本化する予定です(詳細はREADME.md「CHANGELOG.mdの二重管理を避ける方法」
> 参照)。移行後はこのファイルを削除します。

## v0.1.2 (Colab実機テストでの不具合修正・依存バージョン方針の見直し)

- GPUランタイムでない環境で `nvidia-smi` コマンド自体が存在せず
  `FileNotFoundError` になっていた不具合を修正(try/exceptで捕捉しCPU実行に
  フォールバックするよう変更)。
- `requirements.txt` のバージョン指定方針を見直し、当プロジェクトのコードが
  具体的なAPIの形に依存しているパッケージ(pyscf, geometric, ipywidgets)には
  下限に加えて上限も指定(次のメジャーバージョンを自動では取り込まない)、
  それ以外(PyYAML, py3Dmol, plotly)は下限のみとする方針に変更。
- ノートブック生成スクリプト `build_notebook.py` をリポジトリに追加(今後の
  セル追加・修正をコードとして管理できるようにするため)。

## v0.1.1 (Colab実機テストでの不具合修正)

- `requirements.txt` から `numpy` / `scipy` の指定を削除し、他のパッケージも
  `==` の厳密指定から `>=` の最小バージョン指定に変更。Colabのベースイメージと
  異なる厳密バージョンを指定したことでソースビルドが発生し
  `python setup.py egg_info did not run successfully` エラーが出ていた不具合を修正。
- `ColabApp.ipynb` のインストールセルを、1パッケージずつインストールして
  失敗箇所を特定できる方式に変更。
- `ColabApp.ipynb` の「コード本体の取得」セルを、GitHub(`orgchem610/qchem_colab_app`)
  からの `git clone` / `git pull` に一本化(zipアップロード方式は廃止)。
- `qcapp/__init__.py` の `__version__` を `0.1.1` に更新し、
  `qcapp/writer_xyz.py` の出力コメントもこのバージョン番号を動的に参照するよう修正
  (以後、バージョン表記の更新漏れを防止)。

## v0.1.0 (最小プロトタイプ)

- 初回リリース。
- 計算エンジン: PySCF(HFのみ)。GPU4PySCFが利用可能な場合は自動使用、
  不可の場合はCPUへ自動フォールバック。
- 対応基底関数: 3-21Gのみ。
- 構造最適化(geomeTRIC連携)と、既定でONの振動数解析(Hessian・虚振動チェック・
  簡易熱力学量)を実装。
- 入力バリデーション: 原子間距離チェック、電荷とスピン多重度の偶奇整合性チェック、
  遷移金属元素の検出(現時点では未対応のためエラーとして弾く)。
- 出力: 最終構造xyz(ColabReactionへの受け渡し用)、トラジェクトリxyz、moldenファイル。
- 可視化: エネルギー収束(plotly)、最適化軌跡・振動アニメーション・分子軌道・
  電荷密度(py3Dmol)。
- GUI: ipywidgets。
- 設定: `config/*.yaml` に分離済み。

### 既知の制約
- B3LYP等のDFT、6-31G系列等の基底関数、遷移金属用ECP、TS最適化は未実装
  (`config/`にプレースホルダのみ記載)。
- 開発環境のネットワーク制約により、PySCF/GPU4PySCFを実際にインストールしての
  動作確認は未実施(Google Colab上での実機確認が必要)。

<!--
今後のバージョンを追加する際は、上に新しい節を追記してください。テンプレート:

## vX.Y.Z (概要)
- 変更点1
- 変更点2
### 既知の制約
- ...
-->
